import { Button } from "@mui/material";
import React from "react";
import './left-tools.scss';

const LeftTools = (props) => {
  return (
    <div className="left-tools-container">
      <Button>Crop</Button>
    </div>
  );
};

export default LeftTools;
