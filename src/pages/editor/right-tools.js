import { Button } from "@mui/material";
import React from "react";
import "./right-tools.scss";

const RightTools = (props) => {
  return (
    <div className="right-tools-container">
      <Button>Something</Button>
    </div>
  );
};

export default RightTools;
