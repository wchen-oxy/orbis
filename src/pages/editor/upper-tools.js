import { Button } from "@mui/material";
import React from "react";
import './upper-tools.scss';

const UpperTools = (props) => {
  return (
    <div className="upper-tools-container">
      <Button>upper</Button>
    </div>
  );
};

export default UpperTools;
